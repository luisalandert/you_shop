<%= link_to 'Voltar', root_path %>
<%= link_to 'Registrar Empresa', new_company_path %>

<h1>Empresas Cadastradas</h1>

<% @companies.each do |company| %>
  <%= link_to company.name, company %>
<% end %>

<% if @companies.empty? %>
  <p>Nenhuma empresa cadastrada</p>
<% end %>